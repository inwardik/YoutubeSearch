server {
    listen 80;

    location / {
        proxy_pass http://web:8000;
    }

    location /static/ {
        alias /static/;
    }
}

server {
    listen 443;
    server_name _ *;
    rewrite ^(.*) http://$host$1 permanent;
 }